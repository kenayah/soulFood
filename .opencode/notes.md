# Pending Tasks

## Hugo Site
- [x] Fix `.Site.LanguageCode` deprecation → replace with `Site.Language.Locale` in theme partials (already done)
- [x] Fix `.Site.Data` deprecation → use `hugo.Data` instead (already done)

## API Integration
- [ ] Deploy: set `apiBaseURL` in `site/hugo.toml` to the production Workers URL before deployment
