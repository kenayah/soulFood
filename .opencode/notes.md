# Pending Tasks

## Hugo Site
- [ ] Fix `.Site.LanguageCode` deprecation → replace with `Site.Language.Locale` in theme partials
- [ ] Fix `.Site.Data` deprecation → use `hugo.Data` instead (Hugo v0.156.0+)
